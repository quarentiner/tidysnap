{
  "name": "tidysnap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TidySnap MVP: upload a messy-space photo and get three practical organizing actions.",
  "scripts": {
    "start": "node scripts/dev-server.mjs",
    "test": "node --test",
    "build": "node scripts/validate-build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "heic-convert": "^2.1.0"
  }
}
